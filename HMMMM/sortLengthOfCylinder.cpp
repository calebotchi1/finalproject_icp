#include "ShapesBlocks.h"

void SqrBaseRectBlocks::sortLengthOfCylinder(){

sort(length.begin(), length.end());

                cout << "Sorted \n";
                 for (auto x : length)
                   cout << x << endl;
}

