#include "ShapesBlocks.h"
 void cuboidBlocks:: SortAscendingOfDiameterSPhere(){

             sort(W.begin(), W.end());

                cout << "Sorted \n";
                 for (auto x : W)
                   cout << x << endl;

     }
