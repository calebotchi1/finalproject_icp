#include "ShapesBlocks.h"

  void SqrBaseRectBlocks:: printVectorCylindrical(){
               for(int i=0; i<w.size(); i++)
    {
       if(w[i] == h[i]){
            width.push_back(w[i]);
                        height.push_back(h[i]);
                         length.push_back(h[i]);
         cout << w[i] <<endl;

       }



    }



     }
